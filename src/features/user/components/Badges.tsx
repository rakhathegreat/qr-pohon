import React from 'react';

type BadgeProps = {
    id: string;
};

const Badges: React.FC<BadgeProps> = ({ id }) => {
    return (
        <div className="border border-gray-300">
            
        </div>
    );
};

export default Badges;